require 'spec_helper'

describe WorkLogMailer do
  it 'has a version number' do
    expect(WorkLogMailer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
